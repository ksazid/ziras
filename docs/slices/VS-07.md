# VS-07 — 14-Day POC Metrics Validation

Status: Testing — household-value source expansion pending exact-head certification

## Objective

Run the formal Malta discovery-engine POC measurement window against the approved PRD gates without enabling production source access, merchant onboarding, release or production deployment.

## Validation window

- Duration: 14 consecutive calendar days.
- Scheduled window: 2026-09-01 through 2026-09-14.
- Scheduled ingestion: 08:00 Europe/Malta (06:00 UTC), POC scope only.
- Daily brief: follows the ingestion run and reports machine metrics separately from audit metrics.
- Geography: Birkirkara, Sliema, Gżira, St Julian’s, Valletta.
- Source policy: VS-04 baseline + VS-06 POC extensions + the approved VS-07 household-value POC extension.
- Production source access: OFF.
- Release/production enablement: NOT AUTHORIZED.

## PRD gates

| ID | Gate | Pass threshold |
| --- | --- | --- |
| POC-01 | Useful discoveries/day | >= 50/day |
| POC-02 | Valid when opened | >= 90% |
| POC-03 | Stale/expired recommendations | < 5% |
| POC-04 | Duplicates after processing | < 5% |
| POC-05 | Independent source types | >= 5 |
| POC-06 | Relevant to selected interests | >= 70% |
| POC-07 | Merchant onboarding | 0 required |

## Measurement contract

Each accepted daily record must bind to:

- UTC/Malta measurement date;
- exact repository SHA;
- exact GitHub Actions run ID;
- source-level acquisition status and candidate count;
- total candidates, ranked/surfaced discoveries and useful-discovery count;
- valid-open sample count and valid-open count;
- stale/expired surfaced count;
- duplicate count after processing;
- distinct approved source classes contributing candidates;
- relevance sample count and relevant count;
- merchant-onboarding count;
- pass/fail result for every measurable daily gate;
- integrity status so missing/partial evidence fails closed.

## Aggregation rules

- `useful_discoveries_per_day` is evaluated for every accepted day and across the 14-day mean; no fabricated interpolation for missing days.
- validity rate = valid opens / sampled opens.
- stale rate = stale or expired surfaced recommendations / surfaced recommendations checked.
- duplicate rate = duplicates after processing / processed discoveries.
- source-type coverage is the count of independent approved source classes that produced usable discoveries during the window.
- relevance rate = discoveries judged relevant / relevance sample.
- merchant onboarding must remain exactly zero.
- A day with incomplete required evidence is `invalid` and does not silently count as a pass.
- A complete failing day remains evidence and cannot be discarded to cherry-pick a passing window.

## Household-value source expansion — approved before Day 1

Product owner approved the following seven additional POC categories/sources before the formal measurement window begins. Full qualification is recorded in `docs/integrations/VS07-HOUSEHOLD-CATEGORY-QUALIFICATION.md`.

| Priority | Category | POC source | Source class |
| --- | --- | --- | --- |
| P0 | Grocery & supermarket | Lidl Malta Public Offers — POC | `supermarket` |
| P0 | Restaurant / takeaway | AX Hotels Sliema Dining Offers | `restaurant-direct` |
| P1 | Family & kids | Esplora Family Promotions | `family-kids` |
| P0/P1 | Activities | Heritage Malta What's On | `activities-official` |
| P1 | Senior discounts | Active Ageing Malta — Discounts for the Elderly | `senior-benefits-official` |
| P1 | Pharmacy / personal care | Botika Malta Personal Care Sale | `pharmacy-personal-care` |
| P1 | Spa & wellness | AX Verdala Wellness Offers | `spa-wellness` |

Existing POC source classes remain active:

1. `visitmalta_events` — `events-official`
2. `eden_cinemas` — `entertainment-offers`
3. `homemate_offers` — `home-retail`
4. `spazju_kreattiv_events` — `cultural-venue-events`
5. `eurosport_malta_sale` — `sports-retail`

### Expansion controls

- each new POC source has an explicit POC-only allow policy;
- each has a narrow public path set and rate cap;
- robots remains mandatory for web acquisition;
- raw page bodies/images are not retained;
- login/account/cart/checkout/booking/payment flows are excluded;
- every new source has `minimum_candidates: 1`, so HTTP success with zero extracted inventory fails closed;
- the existing research-only Lidl record remains research-only; a separate explicit POC key carries the newly approved narrow permission;
- McDonald’s and Pizza Hut remain denied, Wolt remains partner-only and Franks remains denied;
- Deal.mt is reference/validation inventory only in this batch;
- Corinthia is explicitly not approved for automated acquisition because its published terms prohibit robot/spider/automatic monitoring without prior consent.

## Scheduler controls

The separately approved bounded scheduler is part of VS-07:

- cron: `0 6 1-14 9 *`;
- scheduled runs force `scope=poc`;
- scheduled runs cannot supply a source override;
- a runtime date guard blocks acquisition outside 2026-09-01 through 2026-09-14;
- manual workflow dispatch remains available under existing policy controls;
- production source access is not enabled by the scheduler.

## Acceptance criteria

VS-07 can pass only when:

1. fourteen consecutive complete daily records exist;
2. each record is reproducibly bound to immutable run/SHA evidence;
3. all PRD thresholds are computed from stored evidence;
4. source-policy restrictions remain enforced;
5. production source access remained OFF;
6. the scheduler stayed within the separately approved POC-only window;
7. PES, Discovery Kernel and PostgreSQL/PostGIS/pgvector gates remain green;
8. the final report states PASS or FAIL per gate and does not infer missing observations.

## Test matrix

| ID | Test | Expected result |
| --- | --- | --- |
| TC-01 | Daily measurement schema | required evidence fields validated |
| TC-02 | Missing/partial daily evidence | fail closed / invalid day |
| TC-03 | >=50 useful discoveries | daily gate passes |
| TC-04 | <50 useful discoveries | daily gate fails |
| TC-05 | validity >=90% | pass |
| TC-06 | stale rate <5% | pass |
| TC-07 | duplicate rate <5% | pass |
| TC-08 | >=5 source classes | pass |
| TC-09 | relevance >=70% | pass |
| TC-10 | merchant onboarding >0 | fail |
| TC-11 | 14-day aggregate with <14 complete records | cannot pass |
| TC-12 | exact SHA/run provenance | mandatory |
| TC-13 | production policy restrictions | remain denied/not authorized |
| TC-14 | regression suite | green |
| TC-15 | PostgreSQL integration | green |
| TC-16 | PES | green |
| TC-17 | default catalog loads VS-06 + VS-07 POC extensions | pass |
| TC-18 | each new household source minimum candidates | >=1 |
| TC-19 | old restricted sources unchanged | pass |
| TC-20 | family promotion generic-heading noise | rejected while named promotion retained |
| TC-21 | personal-care sale product card | old/current EUR prices extracted |
| TC-22 | wellness package vs generic voucher heading | package retained; generic heading rejected |
| TC-23 | activities dated event | title/date extracted |

## Scope boundaries

In scope:
- deterministic measurement tooling;
- daily evidence artifacts and 14-day aggregation/reporting;
- bounded POC-only scheduler;
- the approved seven-category household-value POC source expansion;
- extraction hardening required to produce truthful candidates from those source shapes;
- test coverage for measurement, policy and extraction correctness.

Out of scope:
- production source enablement;
- release approval;
- production deployment;
- sources outside the explicitly approved POC pack;
- user-facing feed/UI;
- merchant onboarding.

## Certification history and amendment state

The measurement framework was certified at exact implementation SHA `3bc1341dae7991326d16bd10c5a6804585d1748e` after Discovery Kernel, PES and PostgreSQL/PostGIS/pgvector passed. PR #12 merged at `70ba7d21827f6c48fd4d0ff9db5df51f93aae427`.

The bounded scheduler was separately approved and merged through PR #13 at `6b829a7a612befb002565bbcc859d895c1bb0575` after PES, Kernel and PostgreSQL gates passed.

The household-value source expansion changes runtime acquisition policy/catalog/extraction behavior, so the prior VS-07 certification is intentionally treated as **stale for the current amendment** until the expansion exact head passes the same gates and receives certification approval. The previous certification evidence remains historical evidence; it is not reused as proof for the new code.

## Approval

- Product owner: `ksazid`.
- Measurement setup: explicitly approved with “Go ahead”.
- Measurement framework certification: explicitly approved.
- Bounded daily POC scheduler: explicitly approved.
- Household-value categories/source-policy expansion: explicitly approved with “Approved, lets add these”.
- Production source enablement: NOT APPROVED.
- Release/production enablement: NOT APPROVED.
